# Learning Progress

Curriculum: see `LEARNING.md`.

**Resume convention:** at session start, say "next lesson" — Claude reads this file, finds the next unchecked lesson, and begins.

---

## Current Status

**Next up:** L14 — Hooks (`#hooks`)

---

## Module 0 — Foundations
- [x] L1: First Loop — `#protocol` — one-line copy fix in `lib/constants.ts`
- [x] L2: Diff Review Drill — `#diff-review` `#scope` — read a 50-line change line by line

## Module 1 — Prompt Craft
- [x] L3: Specificity & Constraints — `#prompt-craft` `#constraint-driven`
- [x] L4: Spec-Driven Prompts — `#spec-driven`
- [x] L5: Role Priming — `#role-priming`
- [x] L6: Meta-Prompting — `#meta-prompt`

## Module 2 — Verification, Trust, Red-Teaming
- [x] L7: Trust Calibration — `#trust-calibration` `#verify`
- [x] L8: Hallucination Drill — `#hallucination`
- [x] L9: AI-as-Reviewer — `#red-team` `#second-ai`

## Module 3 — Safety Nets & Context Economy
- [x] L10: Plan Mode & Artifacts — `#recovery` `#artifacts`
- [x] L11: Git + Context Hygiene — `#recovery` `#context-economy`

## Module 4 — Configuring the Harness
- [x] L12: CLAUDE.md as a Programming Surface — `#claude-md`
- [x] L13: Custom Slash Commands — `#commands`
- [ ] **L14: Hooks** ← next — `#hooks`
- [ ] L15: Permissions & Memory — `#permissions` `#memory`

## Module 5 — Multi-Agent Orchestration
- [ ] L16: Spawning Subagents — `#subagent`
- [ ] L17: Parallel Agents + Second-AI Review — `#parallel-agents` `#second-ai`

## Module 6 — Production & Architectural Thinking
- [ ] L18: "How does this break?" — `#failure-mode`
- [ ] L19: Architectural Conversation — `#prompt-craft`
- [ ] L20: ADRs & Spec → Plan → Code — `#artifacts`

## Module 7 — Graduation
- [ ] L21: Final Exam — Email Integration (`app/api/inquiry/route.ts:36`)
- [ ] L22+: Feature-Driven (ongoing)

---

## Session Log

(Claude appends one line per completed session: date, lesson, skill practiced, brief note.)

- 2026-05-05 — L1: First Loop — `#protocol` — Em dash → period in `lib/constants.ts:53` (Wildlife Safaris card). Side trip mid-lesson: built `BRAND.md` and referenced from `CLAUDE.md` (foreshadows L12 `#claude-md`); plus `#meta-prompt` exercise on agentic loop design.
- 2026-05-06 — L2: Diff Review Drill — `#diff-review` `#scope` — Added `components/TrustStrip.tsx` and wired into `app/page.tsx` between Hero and Featured Tours, reinforcing the BRAND.md takeaway thread. ~40 lines across 2 files. Read line-by-line, scope verified clean. Bonus concepts surfaced: utility-first CSS philosophy, Tailwind 4 `@theme` config in `app/globals.css`, flexbox main/cross axis, `group`/`group-hover:` parent-scoped hover, `node_modules` vs project code.
- 2026-05-08 — L3: Specificity & Constraints — `#prompt-craft` `#constraint-driven` — Compared three prompts (vague / specific / specific+constrained) for refactoring home Featured Tours. User caught the meta-flaw: even Prompt C was scoped to a Kili-specific section while the brand spans 5 product lines — Step 0 of #prompt-craft is "is this the right thing to build?" Refactored `FEATURED_TOURS` shape (added `category` + `duration` + `priceFrom`), redesigned `TourCard.tsx`, created `components/FeaturedTours.tsx` as a client component with filter chips (All / Kilimanjaro / Safari) and a horizontal snap-scroll carousel with chevron buttons (disabled-at-edges, hidden on mobile). Multiple plan-mode pivots driven by browser-test feedback: layout flipped from grid→single row→arrows-outside-cards; cards tightened (smaller image, less padding, lower min-h); UI cleanup (removed Starts from / Per person / View Trip pill; added USD prefix). Bonus concepts: `useState`+`useRef`+`useEffect` cleanup, client/server component split (`"use client"`), CSS `scroll-snap`, programmatic `scrollBy`, the `mt-auto` + `min-h` pattern for uniform card heights with bottom-aligned content (and the asymmetric-gap trade-off it forces).
- 2026-05-11 — L4: Spec-Driven Prompts — `#spec-driven` — Side trip first: drafted `AUDIENCE.md` (buyer psychology + 12 conversion principles + trust hierarchy) and committed it before lesson start. Step 0 of the lesson rejected the curriculum-suggested target (`GalleryGrid` empty state — not reachable today) and pivoted to "Why Choose Kilimanjaro True Venture" instead. Wrote 6 ACs *before* drafting copy (content / wedge / Abu Hope tone / 3-card structure / BRAND.md voice exclusions / truth-anchor). User course-corrected three factual overclaims mid-AC drafting — Abu is mid-20s (no veteran framing), shared ownership as values claim not legal claim (don't single Abu out), KTV is for-profit so the foundation is "supported when possible" not "funded by profits." The AC stage absorbed the taste/truth work upfront, so implementation was mechanical: replaced `WHY_CHOOSE_US` (6 cards) → `VALUES` (3 cards) in `lib/constants.ts`, swapped section h2 to *Tanzanian-owned. Tanzanian-guided. Tanzanian-built.*, dropped the subtitle paragraph, changed grid from `md:grid-cols-2 lg:grid-cols-3` → `md:grid-cols-3`, trimmed `ICONS` map (removed shield/package/trophy as dead). Bonus concepts: Intrepid as anti-template (scale-flexing inverted to locality-insistence); the wedge-by-omission move (contrast lands by absence of corporate vocabulary, not by trash-talking competitors); section-purpose reframe (features vs. values is a rhetorical-job distinction); renaming a constant when its name no longer matches its content (`#refactor-discipline` exception — same edit, not "while I'm here").
- 2026-05-11 — L5: Role Priming — `#role-priming` — Compared two reviews of `components/FeaturedTours.tsx` from parallel subagents (preview of `#subagent`/`#parallel-agents` from Module 5): one primed as a senior engineer who hates over-engineering, one as a senior product designer. Near-zero overlap of findings even when both pointed at the same line: at line 20, engineer flagged the `arrowDisabled` constant as over-extracted; designer flagged `cursor-not-allowed` as a lie that reads "broken" rather than "at the end." Engineer voted ship-as-is; designer voted ship with two fixes. Steering-wheel claim confirmed: same file, different prompt, disjoint reports. Attempted Bug 1 fix (silent filter scroll-teleport) with Approach B: a `scrollTo({left:0})` effect on `[filter]` plus an `sr-only` `aria-live="polite"` region with a derived "Showing N tours in X" string. Browser test caught a gap: the reset wasn't visible on All→Safari because Safari's 3 cards (~918px) fit the desktop scroller — browser auto-clamps `scrollLeft` to 0 already, our effect is a no-op, and the visible cards happened to be the same ones (tours 7-9 of the unfiltered list became tours 1-3 of safari). Diagnosis was correct (not a code bug), but the real spec turned out more nuanced than the one-shot patch: transition should fire iff source filter had `scrollLeft > 0`, and for no-overflow destinations the fix needs scroll-then-swap sequencing or a fade — not a single effect. Reverted via `git restore components/FeaturedTours.tsx` to revisit later with a clearer spec. Bonus concepts: prompt negative space ("do NOT comment on X" sharpened both reviews more than the role label); `aria-live="polite"` + `sr-only` pattern for screen-reader-only announcements; browser auto-clamps `scrollLeft` when content shrinks below clientWidth; `git restore` (discards uncommitted changes) vs `git revert` (creates a new inverse commit, preserves history) — the colloquial use of "revert" is technically the former.
- 2026-05-16 — L6: Meta-Prompting — `#meta-prompt` — Pivoted from L5's unfinished filter-transition bug to a greenfield `/gallery` redesign (wider ambiguity surface = stronger L6 target). User's meta-prompt surfaced ~10 ambiguities they hadn't pre-decided across placement / conversion / content / form-factor. Fired `marketing-funnel-strategist` in background (preview of `#second-ai`/`#subagent` from Module 5) — grounded in BRAND.md + AUDIENCE.md, it recommended uniform grid + lightbox + category-tagged tour-page exit link, with a `#failure-mode` flag: gallery-as-destination not doorway. User overruled the agent on form factor (chose masonry) but accepted the exit-link sub-decision. "95% shared vision" interview pass revealed most of it already existed (`components/GalleryGrid.tsx` had hand-rolled lightbox with keyboard nav, click-out, image counter, filter chips) — implementation became a tweak not a build. Surfaced photo-coverage gap (0 photos for Zanzibar/Cultural → shipped 3 chips not 5) and inspected real pixel dimensions via `sips` so masonry could use intrinsic aspect ratios. Implementation prompt as durable artifact: 11 explicit ACs, full data table for 17 photos, code-block change spec — fresh-Claude executability test. 4 edits applied cleanly. Bonus concepts: CSS `columns-N` masonry (flows top-to-bottom within columns, not L→R — irrelevant for non-narrative galleries); `break-inside-avoid` to prevent tile splitting across columns; Next.js Image intrinsic `width`/`height` vs `fill` (masonry needs intrinsic heights, so `fill` is wrong here); `Record<string, ...>` lookup map with safe undefined fallback via `&&` short-circuit; `e.stopPropagation()` on the lightbox bottom div so clicking the Link doesn't trigger backdrop close; `#refactor-discipline` exception for removing `getGridClasses`/`getAspectRatio` (dead after layout swap, not "while I'm here"). The `#meta-prompt` + `#second-ai` stack is roughly the highest-leverage two-skill combo in the curriculum, and you used both without prompting.
- 2026-05-16 — L7: Trust Calibration — `#trust-calibration` `#verify` — Paired tasks for mode-switch practice. Task 1 (high-trust): added 2 testimonials to `TESTIMONIALS` (Mt Meru / Netherlands, Zanzibar / India). Pure data, pattern-replicating, user skimmed and passed in ~10s. Task 2 (low-trust): added real JS validation to `components/InquiryForm.tsx`. User read every line in claimed scrutinize mode and found zero gaps. The scrutiny-found-zero-gaps moment was the lesson — 7 real bugs were present across 3 tiers: error-state lifecycle (errors don't clear on field edit), HTML5-required short-circuits JS for empty fields (two different error styles), email regex too lenient (`a@b.c` passes), `Number()` accepts scientific notation (`1e2` → 100 passes `Number.isInteger`), double-submit race (disabled attr not applied before first re-render), no message length cap, `as Record<string, string>` cast lying about FormData typing. Plus a TS diagnostic on `FormEvent` deprecation as a free finding. Takeaway: trust calibration is two-part — switching modes deliberately (the easy half) AND having a bug-pattern library so scrutiny is productive (takes years; got 7 specimens today). User asked to fix all 3 tiers, then also resolve the FormEvent deprecation. Presented 4 alternatives with tradeoffs (inline anonymous handler / `ComponentProps` lookup / ref-based / suppress); user chose ref-based. Refactored: `useRef<HTMLFormElement>` for direct DOM access; `handleSubmit()` takes no params and reads `formRef.current` instead of `e.currentTarget`; `clearFieldError(name: string)` replaces `handleFieldChange(e: ChangeEvent)`. Events become JSX wiring (3-line inline arrow on `onSubmit`), not data carriers. Bonus concepts: `noValidate` to disable HTML5 bubble-popups so JS validation can own the UI; `/^\d+$/` precheck before `Number()` to reject scientific notation; functional `setState((prev) => ...)` to avoid stale closures when mutating the errors record; refs as React's escape hatch (sticky-note model: empty until mount, then holds the DOM element); the React 19 / @types/react synthetic-event deprecation as an unsettled ecosystem transition (no single idiomatic replacement yet); testability gain from ref-based pattern (named handler is honest about its dependency — the form ref — and can be unit-called without faking events).
- 2026-05-16 — L8: Hallucination Drill — `#hallucination` — Planted one subtle bug in a throwaway `lib/format.ts` containing `formatPriceRange(min, max)`. Bug: `value.toLocaleString(PRICE_LOCALE, { minimumFractionDigits: 2 })` forces `.00` decimals on whole-dollar tour prices ($2,050.00 instead of $2,050). The hallucination fingerprint: AI's training distribution is dominated by USD/EUR price formatting with cents (e-commerce code), so it defaults to "money = 2 decimals" even when the explicit spec showed integer dollars. User caught it cleanly by asking for a Python-bridged TypeScript explainer first (legitimate scrutiny — understand the code *before* judging it), then mentally walked `formatPrice(2050)` through the function and saw the spec mismatch (`$2,050.00` vs spec's `$2,050`). Correct fix proposed: `maximumFractionDigits: 0`. Bonus findings from L7 library firing: negative numbers (valid contract concern — tour prices never negative) and scientific notation (misapplied — input is typed `number`, so the string-parsing boundary from L7 doesn't exist at this layer; the right defensive check here is `Number.isFinite` for NaN/Infinity since both are `typeof === "number"`). Decision: deleted the file (no caller, drill was the point). Bonus concepts: TypeScript-as-typed-Python — `const` (enforced unlike Python convention), template literals as f-strings (with the gotcha that the leading `$` is literal, not interpolation), options-bag pattern as a JS dict-arg equivalent, `===` strict equality, method calls on number primitives (`(2050).toLocaleString()`), `export` opt-in vs Python's public-by-default; `Intl.NumberFormat` option semantics (`minimumFractionDigits` = AT LEAST, not AT MOST — read option names literally, not by guess); `typeof NaN === "number"` gotcha; the principle that **defensive boundaries live where unsafe types cross into safe types** (string→number is one boundary at L7's form validation; typed-number-entry is a narrower one for NaN/Infinity guards); the **AI hallucination fingerprint** to add to the library — "pattern from training distribution overriding the specific spec," especially in domains with strong stylistic defaults (money formatting, date parsing, email regex, pagination).
- 2026-05-17 — L9: AI-as-Reviewer — `#red-team` `#second-ai` — Closed Module 2. Spawned a fresh-context red-team subagent on `components/GalleryGrid.tsx` (shipped in L6 with only AC-level verification, no line-by-line review). I pre-registered 6 predictions; user opted to skip their own predictions. Agent returned 12 findings in ~55s (5 HIGH / 6 MED / 2 LOW). I caught 5/6 of my own predictions (83% self-review accuracy on what I'd written) but the agent surfaced 6 unpredicted findings, several user-impacting: (1) **gallery tiles were not keyboard-operable** — `<figure>` with onClick but no tabIndex/role/key-handler, so keyboard users literally could not open the lightbox at all (catastrophic WCAG 2.1 fail); (2) no body-scroll-lock when modal open (mobile background scrolls through backdrop); (3) backdrop dead zones from `object-contain` + fixed container letterboxing — taps in visible-margin areas around image did nothing; (4) lightbox `priority={true}` unconditional misuse (priority is for LCP, not "main image of the moment"); (5) stale-index race when filter changes while lightbox open; (6) hardcoded `CATEGORIES` could silently drift from data. Decision: fix all 5 HIGH via single-file rewrite. Implementation: `<figure>` → `<button>` with `aria-label`, `focus-visible:ring`; `role="dialog" aria-modal="true" aria-label="Photo viewer"` on backdrop; `lastTriggerRef` + `closeButtonRef` for focus-into-modal-on-open and focus-restore-on-close; Tab/Shift+Tab focus trap via `querySelectorAll('button, [href], [tabindex]:not([tabindex="-1"])')` + `document.activeElement`; body scroll lock via `document.body.style.overflow = "hidden"` with cleanup; image wrapper sized to image's own aspect ratio (no letterboxing dead zones); `priority` dropped from lightbox image (M2 fixed as H5 side effect). Skipped: inert/aria-hidden on siblings (needs Portal restructure; aria-modal covers most SR cases). Follow-up size issue: my H5 fix sized image to intrinsic dimensions, leaving it visibly small on laptops with sources at 1080px. User asked for image-quality tradeoffs. Presented Options A/B/C/D with retina-display math (1080px source × 2x DPR = needs 2160 device pixels for crisp; max-* caps DOWN never UP). User chose B — cap upscale at source resolution via `width: min(95vw, ${sourceWidth}px, calc(85vh * ${aspect}))`. Tradeoff: sharper at all displays but inconsistent size across photos when sources vary; auto-improves when photos upgrade later. Bonus concepts: `aria-modal="true"` puts compatible screen readers in "modal mode" (siblings ignored) without requiring DOM restructure; focus-trap via `document.activeElement` check on Tab key is simpler than ref-arrays; `queueMicrotask` for focus restoration after React unmount; CSS `min()` with multiple values for multi-constraint sizing; the `max-*` semantic — it caps DOWN, never UP — so to scale UP requires explicit dimensions or aspect-ratio + viewport-relative units; display density (DPR) as a separate axis from CSS pixels; Next.js Image `quality` prop is JPEG compression, not resolution. Takeaway: **fresh-context red-team paid for itself** — 5 of 12 (42%) of agent's findings were genuinely user-impacting bugs shipped through casual review. The H2 keyboard-unoperable tile bug is the kind of catastrophic a11y miss self-review almost never catches; that single finding alone justifies the move.
- 2026-05-17 — L13: Custom Slash Commands — `#commands` — Wrote `.claude/commands/review-component.md`, a project-level slash command for fresh-context red-team review of any component file. Framed slash commands as the third programming surface (alongside CLAUDE.md and hooks): where CLAUDE.md is always-on, a slash command is opt-in — a named, parameterized, scope-bounded prompt invocation. File convention: `.claude/commands/<name>.md`, filename = command name, body = prompt template. Frontmatter taught: `description` (picker label), `argument-hint` (UI placeholder), `allowed-tools` (security boundary), `model` (override). `$ARGUMENTS` interpolation maps user input after the command name into the prompt body. Built `/review-component` with `allowed-tools: Read, Bash(ls:*), Bash(rg:*), Bash(grep:*), Glob, Agent` — deliberately omitted Edit/Write so the command can't accidentally apply hallucinated fixes; boundary enforced by harness not by trust. Body has two parts split by `---`: first part is my instructions to me (spawn one general-purpose agent), second part is the prompt the spawned agent receives (read CLAUDE.md + BRAND.md + target file first; review across correctness/bugs/a11y/design-system/brand-voice/performance/conventions; return HIGH/MED/LOW findings with line numbers, why-it-matters, suggested fix, overall assessment). Tested live on `components/InquiryForm.tsx` (open in IDE; previously hardened in L7) — agent returned 18 findings in ~73s with most being **new** beyond L7's validation-logic fixes: HIGH#1 errors not announced to SR (no aria-live, no aria-describedby — WCAG 3.3.1 + 4.1.3 violations), HIGH#2 focus not moved to first invalid field on submit, HIGH#3 success state replaces form with no focus shift or aria announcement, HIGH#4 number input UX accepts decimals/scientific notation that JS validator catches but the input itself displays as valid. L7 fixed *logic*; the form's *a11y* was never thoroughly reviewed. Did NOT fix findings — that's a separate task; the lesson is `#commands` not "patch InquiryForm a11y." Bonus concepts: **DRY-for-prompts heuristic** — if you've typed roughly the same multi-paragraph prompt three times, it should be a slash command; **tool allow-listing as scope enforcement** — `allowed-tools: Read, ..., Agent` (no Edit/Write) means the command literally cannot mutate files, so you can run it speculatively without worrying about a hallucinated fix getting written; **commands as team standardization** — the command file is committed to the repo so it travels with the codebase (future-you on a new machine, or anyone else cloning, gets the same review workflow); **scope boundary as security pattern** — same allow-list mechanism that lives in `settings.json` permissions also applies at the command level, giving you per-command scoping; **project-level vs user-level commands** (`~/.claude/commands/` for cross-project workflows like `/lesson-wrap`, `.claude/commands/` for project-specific knowledge like `/review-component` which needs to know about CLAUDE.md / BRAND.md / the dark theme); **command structure pattern** — body split by `---` into "instructions for orchestrator" + "prompt for spawned agent" lets one command do both meta-planning and the actual work. Future commands hooked but not built: `/brand-check <file>` (narrower copy-only review), `/lesson-wrap` (automate end-of-lesson ritual), `/a11y <file>` (a11y-only narrower review). Takeaway: a slash command pays back its writing cost the third time you invoke it, and the file traveling with the repo means the workflow is durable beyond any single session — same durability principle as L10 plans-as-files, applied to prompts instead of plans.
- 2026-05-17 — L12: CLAUDE.md as a Programming Surface — `#claude-md` — Module 4 opened; code work shifts from app to Claude Code itself. Framed CLAUDE.md as a **prompt prefix that fires every session** — not documentation, a program. The audit lens: contradictions (poison every session) > stale facts (slow poison) > inline duplicates of code-resident data (drift) > vague rules (overhead) > specific rules with a *why* (the good stuff). Audited all 379 lines, surfaced 6 findings F1-F6 with file:line anchors. Applied 5 fixes; held F5 (most subjective) for later. **F1 — Outright contradiction**: `CLAUDE.md:206` Testing Checklist said "Page looks modern and vibrant (not dark)" while Design System and "Things to AVOID" both insisted on dark theme; same drift on line 18 ("modern, vibrant travel website") and line 377 ("Current Theme: Modern, Vibrant, Nature-Focused"). All three were pre-dark-redesign leftovers actively fighting Design System every session. Fixed all three. **F2 — Inline copies of code-resident data**: Contact Info (lines 138-144), Pricing Structure (lines 148-153), and Image catalog (lines 157-163) all listed values that live in `lib/constants.ts` / `/public/images/`. Each *also* told Claude to use the source-of-truth file, so the inline list was redundant AND drift-prone. Replaced with pointers. **F3 — Learning & Education bloat**: 70-line section (lines 248-313) explained how-Claude-teaches with a "Bad/Good" example, a 12-row concepts table, and a "your learning responsibility" list — all duplicated by LEARNING.md + PROGRESS.md, with one of the worked examples even using a light-theme example (`text-light → text-gray-600` on white background) that *itself* contained the F1 contradiction. Collapsed to a 3-line pointer at LEARNING.md/PROGRESS.md. Biggest single context savings of the session. **F4 — Wishlist**: "Next Steps / Future Features" section (lines 362-372) was 8 bullets of potential enhancements — not an instruction to Claude, didn't change behavior. Deleted. **F6 — BRAND.md duplication**: Writing & Copy Standards (lines 95-121) said "BRAND.md is source of truth" then re-listed 6 anti-patterns and 4 worked examples from BRAND.md. Trimmed to a 1-line pointer. **What I deliberately did not do**: did not add new rules. Your feedback memory has ~7 patterns (#refactor-discipline exception, #show-levers, trust calibration discipline) that could be codified, but adding rules to a bloated doc dilutes signal — trim first, add second. Result: 378 → 264 lines (-30%, -114 net). Bonus concepts: **the pointer pattern** as the cure for inline duplication (when content lives in another source of truth, CLAUDE.md should point at it not copy from it — two copies of the same content guarantees divergence over time); **earned space** principle (a bloated CLAUDE.md dilutes signal so any *new* rule won't fire reliably — you have to earn space for new rules by trimming dead ones first); contradictions are the worst CLAUDE.md failure mode because they actively make future Claude *worse than no instruction at all*; the cost asymmetry — adding to CLAUDE.md feels free (you don't see the per-session token cost) but every line is paid for in context budget every conversation. Takeaway: **CLAUDE.md is the highest-leverage programming surface in Claude Code.** A single contradiction fixed once propagates to every future session, every future Claude, every future task. F1's three-line fix is worth more than most feature commits.
- 2026-05-17 — L11: Git + Context Hygiene — `#recovery` `#context-economy` — Closed Module 3. Hands-on drills (no app code shipped; the deliverable is invisible/internal). **Drill 1**: walked `git reset` through `--soft` / `--mixed` / `--hard` against a throwaway `scratch-reset-drill.md` commit with all three layers populated (HEAD / index / working tree). Each mode demoed what moves and what stays: --soft slides committed content down to index (squash use case), --mixed (default) unstages but keeps working tree (redo-commit-boundary use case), --hard wipes all three (destructive). Then demoed reflog recovery: `git reset --hard 42de2be` resurrected the "destroyed" commit from `git reflog` — committed work is recoverable for ~30 days even after --hard, but uncommitted staged/unstaged layers are gone forever. **Drill 2**: `git worktree add ../kilimanjaro-experiment -b experiment/footer-redesign` created a sibling working directory; proved file isolation (FOOTER_NOTES.md present in experiment dir, absent in main); cleaned up with `git worktree remove --force`. **Drill 3**: pure conversation — pollution signals (stale file refs, contradictions, lengthening responses, scrolling-up-to-remember), summarize-before-clear pattern (PROGRESS.md line / plans/ file / memory entry as durability anchors before `/clear`), subagent-vs-/clear decision rule (subagent for parallel work, /clear for sequential), and the "fresh session" nuclear option for cross-repo or mode switches. Three self-test scenarios at the end. Bonus concepts: the three-layer git model (HEAD/index/working tree) as the unifying mental model — every reset/checkout/restore command is just a different vector through those three layers; reflog as per-ref position history (every HEAD movement logged for ~30 days, configurable via `gc.reflogExpire`); `--force` on worktree removal as the same protection philosophy as `--hard` (git refuses destructive moves unless you opt in); the Claude-side analogue of git safety nets — artifacts (PROGRESS.md / plans/ / memory) are to context what commits are to working trees, both being durability anchors that let you wipe volatile state without fear; the "commit early, even ugly WIP commits" rule because WIP commits create reflog anchors. Takeaway: the test that this lesson stuck isn't a passed checkbox — it's future you not flinching when Claude proposes a destructive operation, because you know exactly how to undo it.
- 2026-05-17 — L10: Plan Mode & Artifacts — `#recovery` `#artifacts` — Module 3 opened. Drafted the plan as a durable markdown file (`plans/L10-gallery-followups.md`) BEFORE any code edits, then implemented from the approved plan. Target: the 6 deferred L9 M-tier and L-tier findings — M1 (swipe + 44×44 tap targets), M3 (`motion-safe:` prefix on hover scale), M4 (`priority={index < 2}` on first masonry tiles for LCP), M5 (`e.preventDefault()` on lightbox arrow keys), M6 (`?? null` defensive clamp on currentImage), L2 (derive CATEGORIES from data). Plan structure: goal / scope (in vs out) / per-item approach with file:line refs and code sketches / order of edits / test plan / risks / approval checkpoint. Plan committed alongside the implementation as audit-trail artifact. Implementation followed plan order exactly: L2 → M3 → M4 → M5 → M6 → M1 (mechanical changes first, swipe last as the biggest logic add). Bonus concepts: Tailwind's `motion-safe:` prefix as a CSS media-query escape hatch (`@media (prefers-reduced-motion: no-preference)`); `priority={index < 2}` as the canonical Next.js Image LCP pattern (top-of-fold eager, rest lazy); swipe-gesture math with vertical-delta guard (`|dy| < 50` filters out vertical scroll attempts → reduces iOS edge-swipe-back conflict); `?? null` defensive clamping at array-access boundaries (collapses `undefined` to a falsy guard the render branch already handles); `Array.from(new Set(...))` for unique-preserving-insertion-order arrays (vs `[...new Set(...)]` — same thing, stylistic choice); WCAG 2.5.5 touch-target minimum (44×44) achieved via `p-3` on icon buttons (32px SVG + 24px padding = 56×56 hit area). Takeaway: **plans-as-files outperform plans-in-conversation** when the work has 3+ items or non-trivial ordering. The plan is the recovery anchor — `/clear`, session crash, or `git reset --hard` doesn't lose it. Plan-mode (Shift+Tab) and plans-as-files are complementary: plan-mode forces the no-edit constraint; the file makes the plan portable across sessions. The discipline scales: even a single-page Markdown plan can replace 20 minutes of "wait, what were we doing?" at session start.
